BROKER SCHEMA mqsi

CREATE FILTER MODULE RecordDistributor_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		DECLARE fileName CHARACTER;		
		SET fileName = LocalEnvironment.File.Name;
		IF CONTAINS(fileName,'.csv2') THEN
			RETURN TRUE;
		ELSE			
			RETURN FALSE;
		END IF;
	END;

END MODULE;
